services:
  dashboard:
    image: nginx:alpine
    volumes:
      - ./cecfo-dashboard.html:/usr/share/nginx/html/index.html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
    networks:
      - cloudflared

networks:
  cloudflared:
    external: true